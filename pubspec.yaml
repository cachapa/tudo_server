name: tudo_server
description: Server counterpart to the tudo app
version: 1.0.0
homepage: https://github.com/cachapa/tudo_server

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.7.0
  crdt_sync: ^1.0.10
#    path: ../../crdt/crdt_sync
  http: ^1.4.0
  postgres_crdt: ^3.0.3
#    path: ../../crdt/postgres_crdt
  rxdart: ^0.28.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3
  version: ^3.0.2

dev_dependencies:
  lints: any
  test: any
